# core/lcap/spec.py

LCAP_FORMAT = "lcap"
LCAP_VERSION = 1

DEFAULT_META = {"draft": True}
DEFAULT_SETTINGS = {
    "normalize_punct_spacing": True,
    "disable_highlight_at_out": True,
    "default_style": "Normal",
}