# LumiCap Studio 💜

A modular caption engine with native `.lcap` format.

## Features
- SRT Import
- Grouping Engine
- Style System
- Protect Logic
- LCAP Export
- Draft Metadata

## Format
Native project format: `.lcap`
Versioned with `format` and `version` header.